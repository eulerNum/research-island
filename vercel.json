{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "ignoreCommand": "if [ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ]; then exit 0; fi; git diff --quiet $VERCEL_GIT_PREVIOUS_SHA HEAD -- . ':(exclude)data' && exit 1 || exit 0"
}
